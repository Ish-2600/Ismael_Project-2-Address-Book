# HOW TO RUN:
1. clone project
2. go to project root and run `make all`
3. run ` ./addressbook`

# HOW TO RUN TEST FILES:
1. run `make debug1`, then `debug1` for (add_contact(), search_contact())
2. run `make debug2`, then `debug2` for (get_option())
3. run `make debug3`, then `debug3` for (list_contacts())
